# Sebastian Salazar Portfolio

Simple React and Vite portfolio site deployed to GitHub Pages.

## Local Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The production site is built from `main` by the GitHub Pages workflow in `.github/workflows/deploy.yml`.
